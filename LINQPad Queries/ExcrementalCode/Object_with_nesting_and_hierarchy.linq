<Query Kind="Program" />

void Main()
{
	var conversationTree = new object[]{ "alpha", "beta", 
		new object[]{ "beta", "alpha" },
		 new object[]{ "gamma", "beta", "alpha" }
	}
}

// Define other methods and classes here
